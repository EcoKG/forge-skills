{
  "name": "forge-skills",
  "version": "7.0.0",
  "private": true,
  "description": "Forge — Context-Engineered Autonomous Development System",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
